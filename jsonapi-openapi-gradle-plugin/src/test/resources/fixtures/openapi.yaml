openapi: 3.1.0
info:
  title: Fixture API
  version: 1.0.0
paths:
  /api/items:
    get:
      x-entity-schema: schemas/items.yaml
      x-operation: find-request
      responses:
        '200':
          description: OK
